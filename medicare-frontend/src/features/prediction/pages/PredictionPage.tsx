/**
 * PredictionPage — Now renders the Unified Clinical Investigation Workspace
 * The old symptom-form → dashboard pattern is replaced by an immersive
 * clinical investigation experience.
 */
import UnifiedClinicalWorkspace from "@/features/unified-clinical-workspace/UnifiedClinicalWorkspace";

export default function PredictionPage() {
  return <UnifiedClinicalWorkspace />;
}
